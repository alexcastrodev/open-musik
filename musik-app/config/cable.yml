development:
  adapter: redis
  url: <%= ENV.fetch("REDIS_URL", "redis://localhost:6379/1") %>

test:
  adapter: test

production:
  adapter: redis
  url: <%= ENV.fetch("REDIS_CABLE_URL", ENV.fetch("REDIS_URL", "redis://localhost:6379/1").sub(/\/\d+$/, "/1")) %>
